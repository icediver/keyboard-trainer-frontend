export const typingText: string =
	'Cool Switch\n' +
	'async await () => {};\n' +
	'async await () => {};\n' +
	'async await () => {};\n' +
	'async await () => {};\n' +
	'&$&$&$&$& #3abc56 #fff #abcdef;\n' +
	'&$&$&$&$& #3abc56 #fff #abcdef;\n' +
	'&$&$&$&$& #3abc56 #fff #abcdef;\n' +
	'&$&$&$&$& #3abc56 #fff #abcdef;\n' +
	'const useEffect(() => {}, []);\n' +
	'const useEffect(() => {}, []);\n';
// 'const useEffect(() => {}, []);\n' +
// 'const useEffect(() => {}, []);\n' +
// 'const const const const\n' +
// 'const const const const\n' +
// 'const const const const\n' +
// 'const const const const\n' +
// 'const const const const\n' +
// 'user admin user admin user admin\n' +
// 'user admin user admin user admin\n' +
// 'user admin user admin user admin\n' +
// 'user admin user admin user admin\n' +
// 'user admin user admin user admin\n' +
// 'replace useState by useReducer and pass the dispatch function down to\n' +
// 'components. This will provide better encapsulation, as the state\n' +
// 'manipulation logic is now centralized in a pure reducer and child\n' +
// 'components cannot manipulate it directly anymore via setState. Another\n' +
// 'very good alternative to separate UI Logic from domain logic is to\n' +
// 'provide a custom useAppContext Hook instead of using\n' +
// 'useContext(AppContext) - see previous example. Now useAppContext can\n' +
// 'provide a more narrow API without publishing your whole context.Switch\n' +
// 'to useReducer and/or custom useAppContext Hook Consider to replace\n' +
// 'useState by useReducer and pass the dispatch function down to\n' +
// 'components. This will provide better encapsulation, as the state\n' +
// 'manipulation logic is now centralized in a pure reducer and child\n' +
// 'components cannot manipulate it directly anymore via setState. Another\n' +
// 'very good alternative to separate UI Logic from domain logic is to\n' +
// 'provide a custom useAppContext Hook instead of using\n' +
// 'useContext(AppContext) - see previous example. Now useAppContext can\n' +
// 'provide a more narrow API without publishing your whole context.';
